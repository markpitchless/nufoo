#include <ros/ros.h>

namespace [% package %] {

class [% class %] {

protected:
    ros::NodeHandle nh_;

public:
    [% class %]()
      : nh_("~")
    {
        // Setup ROS topics and services
    }

    void run ()
    {
        ros::spin();
        //while (ros::ok())
        //{
        //}
    }

protected:

};

} //[% package %]

int
main (int argc, char** argv)
{
  ros::init (argc, argv, "[% name %]");
  [% package %]::[% class %] node;
  node.run();
  return 0;
}
